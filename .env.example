# This app is local-only in this iteration: all expense data is stored on
# the device via AsyncStorage, and there are no external API calls yet.
# No environment variables are required.
#
# This file is kept as a placeholder per project convention so future
# iterations (e.g. cloud sync) have a documented place to list new variables.
